import React from "react";
import ScrollContainer from "../ScrollContainer";

function ScrollPage() {
  return (
    <div className="h-screen background-gray-500">
      <ScrollContainer />
    </div>
  );
}

export default ScrollPage;
